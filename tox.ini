[tox]
envlist = py27,py36,coverage

[testenv]
commands =
   python3 -m unittest discover tests

[testenv:coverage]
passenv = TRAVIS TRAVIS_*
commands =
    coverage run --source=tests,algorithms setup.py test
    coveralls
deps =
    coveralls
